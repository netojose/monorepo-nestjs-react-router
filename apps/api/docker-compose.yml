name: namespace_api

services:
  postgres:
    # postgis/postgis has no arm64 build; imresamu/postgis is a multi-arch mirror kept in sync with upstream.
    image: imresamu/postgis:17-3.5
    container_name: namespace_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: platform
      POSTGRES_PASSWORD: platform
      POSTGRES_DB: platform
    ports:
      - '${POSTGRES_EXTERNAL_PORT:-5432}:5432'
    volumes:
      - postgres_namespace_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U platform -d platform']
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    container_name: namespace_redis
    image: redis:8-alpine
    restart: unless-stopped
    command: redis-server --appendonly yes
    ports:
      - '${REDIS_EXTERNAL_PORT:-6379}:6379'
    volumes:
      - redis_namespace_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: ../..
      dockerfile: apps/api/Dockerfile
      target: dev
    container_name: namespace_api
    env_file:
      - .env
    environment:
      DB_HOST: postgres
      CHOKIDAR_USEPOLLING: 'true'
    ports:
      - '${APP_EXTERNAL_PORT:-3000}:3000'
    volumes:
      - ../../apps/api:/app/apps/api
      - /app/apps/api/node_modules
    depends_on:
      postgres:
        condition: namespace_healthy

volumes:
  postgres_namespace_data:
  redis_namespace_data:
